# ── Unity 생성물 ──
# 에디터가 Assets/Packages로부터 다시 만들어내는 것들. 커밋하면 안 된다.
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
/[Mm]emoryCaptures/
/[Rr]ecordings/

# Unity가 자동 생성하는 솔루션/프로젝트 파일
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# 빌드 산출물
*.apk
*.aab
*.unitypackage
*.app
ExportedObj/
.consulo/
crashlytics-build.properties
sysinfo.txt

# ── 이 프로젝트 ──
# 배치모드 빌드 로그. 매 실행마다 갱신되는 산출물이다.
/*.log

# TextMesh Pro 기본 리소스 (4MB).
# com.unity.ugui에 포함돼 있어 Unity가 필요할 때 Assets/로 다시 풀어놓는다.
# 우리 코드/씬 어디에서도 참조하지 않으므로 저장소에 둘 이유가 없다.
# 나중에 TMP로 UI를 만들면 그때 이 줄을 지우고 커밋한다.
/Assets/TextMesh Pro/
/Assets/TextMesh Pro.meta

# ── OS / 에디터 ──
.DS_Store
Thumbs.db
desktop.ini
.vs/
.vscode/
.idea/

# ── Unity .meta 규칙 ──
# 무시된 폴더 안의 .meta는 같이 무시하되, 추적 중인 에셋의 .meta는 반드시 커밋한다.
# .meta가 빠지면 참조(GUID)가 끊겨 씬이 깨진다.
/[Ll]ibrary.meta
/[Tt]emp.meta
/[Bb]uild.meta
/[Ll]ogs.meta
/[Uu]ser[Ss]ettings.meta
